org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.configuration-cache=false
org.gradle.configuration-cache.problems=warn
android.useAndroidX=true
kotlin.code.style=official
#
# Compilation Kotlin sans démon séparé : évite souvent dans l IDE les erreurs
# « Could not connect to Kotlin compile daemon » (panneau Build vide ou sync bloquée).
kotlin.compiler.execution.strategy=in-process
#
# Force un flux de logs simple/visible dans les consoles IDE.
org.gradle.console=plain
org.gradle.logging.level=lifecycle
#
# Sous Linux/WSL : utiliser JAVA_HOME (JDK 21). Sous Windows/Android Studio : si la sync
# Gradle echoue sans JDK, lancer scripts/windows/ensure-windows-studio-gradle-jvm.ps1
# (ecrit org.gradle.java.home dans %USERPROFILE%\.gradle\gradle.properties).
#
