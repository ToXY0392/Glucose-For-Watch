<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-feature android:name="android.hardware.type.watch" />

    <application
        android:name=".GlucoseForWatchWearApplication"
        android:allowBackup="false"
        android:icon="@drawable/ic_wear_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_wear_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.GlucoseForWatch.Wear">

        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="false" />

        <activity
            android:name=".WearMainActivity"
            android:exported="true"
            android:taskAffinity=""
            android:theme="@style/Theme.GlucoseForWatch.Wear">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".tile.GlucoseRefreshActivity"
            android:excludeFromRecents="true"
            android:exported="true"
            android:launchMode="singleTop"
            android:noHistory="true"
            android:taskAffinity=""
            android:theme="@style/Theme.GlucoseForWatch.RefreshProxy" />

        <service
            android:name=".services.WearDataLayerListenerService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.DATA_CHANGED" />
                <data
                    android:host="*"
                    android:path="/glucose/latest"
                    android:scheme="wear" />
            </intent-filter>
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.DATA_CHANGED" />
                <data
                    android:host="*"
                    android:path="/glucose/refresh/status"
                    android:scheme="wear" />
            </intent-filter>
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.DATA_CHANGED" />
                <data
                    android:host="*"
                    android:path="/watch/status/request"
                    android:scheme="wear" />
            </intent-filter>
        </service>

        <service
            android:name=".complication.GlucoseComplicationService"
            android:exported="true"
            android:icon="@drawable/ic_complication_provider"
            android:label="@string/complication_provider_label"
            android:permission="com.google.android.wearable.permission.BIND_COMPLICATION_PROVIDER">
            <intent-filter>
                <action android:name="android.support.wearable.complications.ACTION_COMPLICATION_UPDATE_REQUEST" />
            </intent-filter>

            <meta-data
                android:name="android.support.wearable.complications.SUPPORTED_TYPES"
                android:value="SHORT_TEXT,LONG_TEXT,RANGED_VALUE" />
            <meta-data
                android:name="android.support.wearable.complications.UPDATE_PERIOD_SECONDS"
                android:value="300" />
            <meta-data
                android:name="androidx.wear.watchface.complications.datasource.SUPPORTED_TYPES"
                android:value="SHORT_TEXT,LONG_TEXT,RANGED_VALUE" />
            <meta-data
                android:name="androidx.wear.watchface.complications.datasource.UPDATE_PERIOD_SECONDS"
                android:value="300" />
        </service>

        <service
            android:name=".tile.GlucoseSimpleTileService"
            android:description="@string/tile_glucose_simple_description"
            android:exported="true"
            android:icon="@drawable/tile_glucose_preview"
            android:label="@string/tile_glucose_simple_label"
            android:permission="com.google.android.wearable.permission.BIND_TILE_PROVIDER">
            <intent-filter>
                <action android:name="androidx.wear.tiles.action.BIND_TILE_PROVIDER" />
            </intent-filter>
            <meta-data
                android:name="androidx.wear.tiles.PREVIEW"
                android:resource="@drawable/tile_glucose_preview" />
        </service>

    </application>
</manifest>
