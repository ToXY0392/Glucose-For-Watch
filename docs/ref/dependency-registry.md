# Registry dépendances

## Source

Versions centralisées dans `gradle/libs.versions.toml` (et fichiers build associés).

## Règles simples

- Garder des versions stables.
- Mettre à jour une dépendance à la fois.
- Rebuild mobile + wear après chaque upgrade.
